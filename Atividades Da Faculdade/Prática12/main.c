#include <stdio.h>
#include "funcoes.c"
#include<string.h>
#include <stdbool.h>
//Miguel Henrique Martins Carvalho
//766634
int main(void) {
    /*
    //Fatorial
    int num;
    printf("Digite o numéro\n");
    scanf("%d",&num);
    printf("O número %d tem como Fatorial %d",num,fatorial(num));
   */

    /*Strlen = tamanho da Palavra
    char teste[]= "Miguel";
    int tamanho = strLenght(teste);
     printf("A Palavra %s tem %d Letras",teste,tamanho);
     */

    /*
    //Tamanho dos números

    int num;

    printf("Qual será o número?");

    scanf("%d", &num);

    printf("O Número %d tem %d Dígitos" ,num, contarDigitos(num));

    return 0;
    */
    /*
    //Fibonacci
    int num;
    printf("Qual será o número da série fibonacci?");
    scanf("%d",&num);

    printf("A série será %d", fibonacci(num));
    */
    /*
    int num;

    printf("Qual será o número?");

    scanf("%d", &num);

    printf("O Número %d tem %d Dígitos" ,num, contarDigitos_recursivo(num));

    return 0;
     */


}
/*
 //Palindromo
char palavra[100];

printf("Digite uma palavra: ");
scanf("%s", palavra);

int resultado = isPalindromo_recursivo(palavra, strlen(palavra));
if (resultado) {
printf("A palavra %s é um palíndromo.\n", palavra);
} else {
printf("A palavra %s não é um palíndromo.\n", palavra);
}

return 0;

 */

/*
char palavra[100];

printf("Digite uma palavra: ");
scanf("%s", palavra);

if (eh_palindromo(palavra)) {
printf("A palavra %s é um palíndromo.\n", palavra);
} else {
printf("A palavra %s não é um palíndromo.\n", palavra);
}

return 0;

 */